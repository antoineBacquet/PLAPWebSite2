
<div id="menunavbar" class="collapse navbar-collapse">


    <ul class="nav navbar-nav">
        <div class="navbar-header">
            <a class="navbar-brand" href="http://plapcorp.com"><img class="logo"  src="/img/logo.png"></a>
        </div>

        <li><a class="nav-link" href="{{ path('recruitment') }}" >Recrutement</a></li>

            {% if is_granted('ROLE_MEMBER') or is_granted('ROLE_APPLY') %}
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> Mon profil <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('profile') }}" >Mon profil</a></li>
                        <li><a class="dropdown-item" href="{{ path('myapis') }}" >Mes API</a></li>
                        {% if is_granted('ROLE_MEMBER') %}
                            <li><a class="dropdown-item" href="{{ path('asset') }}" >Mes Asset</a></li>
                            <li> <a href="{{ path('emailsmenu') }}"> Eve-mails </a> </li>
                        {% endif %}
                    </ul>
                </li>

            {% endif %}

        {% if is_granted('ROLE_MEMBER') %}
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> Fit <span class="caret"></span></a>
                <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="{{ path('fit-list') }}" >Liste des doctrines</a></li>
                    <li> <a href="{{ path('fit-skill-bar-index') }}"> Progression des fits</a> </li>
                    {% if is_granted('ROLE_FIT') %}
                        <li><a class="dropdown-item" href="{{ path('fit-add') }}" >Ajouter un fit</a></li>
                        <li><a class="dropdown-item" href="{{ path('fit-admin') }}" >Gestion des doctrines</a></li>
                    {% endif %}
                </ul>
            </li>

        {% endif %}

            {% if is_granted('ROLE_MEMBER') %}
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> Commandes Prod. <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('commandlist') }}" >Liste des commandes</a></li>
                        <li><a class="dropdown-item" href="{{ path('commandadd') }}" >Ajouter une commande</a></li>
                        <li><a class="dropdown-item" href="{{ path('usercommandlist', { 'id' : app.user.id } ) }}" >Mes commands</a></li>
                    </ul>
                </li>
            {% endif %}


            {% if is_granted('ROLE_MEMBER')%}
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> Services <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('discordservice') }}" >Discord</a></li>
                        <li><a class="dropdown-item" href="{{ path('logi') }}" >Service logistic</a></li>
                        <li><a class="dropdown-item" href="{{ path('logi-routes') }}" >Liste des routes</a></li>
                    </ul>
                </li>


            {% endif %}


            {% if is_granted('ROLE_ADMIN')%}
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> Administration <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('members') }}" >Gestion des utilisateurs</a></li>
                        <li><a class="dropdown-item" href="{{ path('members-missing') }}" >API manquantes</a></li>
                        <li><a class="dropdown-item" href="{{ path('apply-list') }}" >Voir les application</a></li>
                    </ul>
                </li>


                <li>
                    <a href="{{ path('discutea_forum_homepage') }}"> Forum </a>
                </li>


            {% endif %}
    </ul>
        <ul class="nav navbar-nav navbar-right">
            {% if app.user %}
                <li  class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                        <img src="https://imageserver.eveonline.com/Character/{{ app.user.charId }}_32.jpg">
                        {{ app.user.name }} <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="nav-link" href="{{ path('logout') }}" >Se deconnecter</a></li>
                    </ul>
                </li>
            {% else %}
                <li><a class="nav-link" href="{{ path('login') }}" >Se connecter</a></li>
            {% endif %}
        </ul>


</div>
